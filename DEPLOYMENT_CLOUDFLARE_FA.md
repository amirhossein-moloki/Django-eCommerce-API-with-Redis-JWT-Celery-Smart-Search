# راهنمای استقرار (Deployment) و تنظیمات کلادفلر (Cloudflare)

این راهنما مراحل لازم برای راه‌اندازی پروژه روی سرور و تنظیمات مربوط به Cloudflare و دریافت گواهی SSL (Let's Encrypt) را توضیح می‌دهد.

> **نکته بسیار مهم:** تمام مراحل فنی دریافت و تمدید گواهی SSL به صورت **کاملاً خودکار** توسط سیستم انجام می‌شود و شما پس از انجام تنظیمات اولیه کلادفلر و فایل `.env` نیاز به انجام هیچ کار دستی دیگری روی سرور نخواهید داشت.

## ۱. تنظیمات DNS در کلادفلر
قبل از هر چیز، باید دامنه خود را به IP سرور متصل کنید:
- وارد پنل Cloudflare شوید.
- به بخش **DNS -> Records** بروید.
- دو رکورد از نوع **A** اضافه کنید:
    1. نام: `@` (برای دامنه اصلی)، مقدار: `IP سرور شما`.
    2. نام: `www` (در صورت نیاز)، مقدار: `IP سرور شما`.
- **بسیار مهم:** در زمان اولین استقرار و برای دریافت موفقیت‌آمیز گواهی SSL، وضعیت ابر (Proxy status) را برای هر دو رکورد روی حالت **DNS Only (ابر خاکستری)** قرار دهید. Cloudflare در حالت پروکسی (ابر نارنجی) ممکن است در فرآیند تایید هویت Let's Encrypt اختلال ایجاد کند.

## ۲. تنظیمات SSL/TLS در کلادفلر
- به بخش **SSL/TLS -> Overview** بروید.
- حالت تنظیمات را روی **Full (Strict)** قرار دهید. این گزینه امن‌ترین حالت است و به این معنی است که هم ارتباط کاربر با کلادفلر و هم ارتباط کلادفلر با سرور شما دارای گواهی معتبر SSL است.

## ۳. آماده‌سازی فایل تنظیمات (.env)
در سرور خود، فایل `.env` را بر اساس `.env.example` ایجاد کرده و مقادیر زیر را حتماً تنظیم کنید:
- `DOMAIN=yourdomain.com` (دامنه خود را بدون http یا www وارد کنید)
- `EMAIL=your-email@example.com` (برای ثبت در Let's Encrypt و دریافت هشدارهای انقضا)
- `CERTBOT_ENABLED=true`

## ۴. اجرای پروژه
پس از انجام تنظیمات، دستور زیر را برای ساخت و اجرای کانتینرها به صورت پس‌زمینه اجرا کنید:
```bash
sudo docker compose up --build -d
```
سیستم به صورت هوشمند در اولین اجرا تشخیص می‌دهد که گواهی SSL وجود ندارد و مراحل زیر را طی می‌کند:
1. سرویس Nginx را موقتاً روی پورت ۸۰ بالا می‌آورد.
2. درخواست گواهی را به Let's Encrypt ارسال می‌کند.
3. پس از دریافت موفقیت‌آمیز گواهی، تنظیمات HTTPS را فعال کرده و Nginx را بازنشانی (Reload) می‌کند.

## ۵. بررسی وضعیت
برای مشاهده روند دریافت گواهی و اطمینان از عدم وقوع خطا، دستور زیر را اجرا کنید:
```bash
sudo docker compose logs -f nginx
```
اگر در لاگ‌ها عبارت `Restoring HTTPS config` یا پیامی مبنی بر موفقیت Let's Encrypt مشاهده کردید، یعنی همه‌چیز به درستی انجام شده است.

## ۶. فعال‌سازی نهایی پروکسی کلادفلر
پس از اینکه سایت با پروتکل HTTPS به درستی در مرورگر باز شد:
- به پنل کلادفلر برگردید.
- وضعیت ابر (Proxy status) رکوردهای DNS را از خاکستری به **Proxied (ابر نارنجی)** تغییر دهید.
- حالا سایت شما از قابلیت‌های امنیتی، کش و CDN کلادفلر بهره‌مند است.

## تمدید گواهی
سیستم به گونه‌ای پیکربندی شده است که گواهی‌ها را به صورت خودکار تمدید می‌کند. یک Cron Job در داخل کانتینر Nginx تعبیه شده که هر شب وضعیت انقضا را بررسی کرده و در صورت نیاز گواهی را تمدید و Nginx را بازنشانی می‌کند.
